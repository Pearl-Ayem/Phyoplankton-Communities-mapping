function H = shannonWiener(pCounts)
H=0;
mask= ~isnan(pCounts) && phyNan~=0;
    for i = [1:length(pCounts)]
        p_i = pCounts(i)/sum(pCounts(mask));
        H_i = p_i * log(p_i);  
        H = H - H_i; 
    end